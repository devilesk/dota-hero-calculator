{
  "name": "dota-hero-calculator",
  "version": "1.0.0",
  "private": true,
  "description": "Dota 2 Hero Calculator",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/devilesk/dota-hero-calculator.git"
  },
  "author": "devilesk",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/devilesk/dota-hero-calculator/issues"
  },
  "scripts": {
    "browserify:dev": "browserify -d -t brfs -u 'knockout' -u 'jquery' --standalone DotaHeroCalculator src/js/main.js > www/bundle.js",
    "browserify:prod": "browserify -d -t brfs -u 'knockout' -u 'jquery' -p [minifyify --map bundle.min.js.map --output build/bundle.min.js.map] --standalone DotaHeroCalculator src/js/main.js > build/bundle.min.js",
    "preprocess:dev": "NODE_ENV=dev node scripts/preprocess.js",
    "preprocess:prod": "NODE_ENV=prod node scripts/preprocess.js",
    "www:dev": "npm run browserify:dev && npm run preprocess:dev",
    "clean:build": "rm -rf build/*",
    "build:css": "mkdir -p build/css && cleancss --skip-rebase -o build/css/app.min.css www/css/*.css",
    "build:img": "mkdir -p build/img && imagemin www/img/* --out-dir=build/img",
    "build:js": "node scripts/browserify.js",
    "build:copy": "cp www/*.php build/ && cp www/*.txt build/ && npm run chmod",
    "build:html": "npm run preprocess:prod",
    "build": "npm run clean:build && npm run build:js && npm run build:css && npm run build:img && npm run build:copy && npm run build:html",
    "chmod": "node scripts/chmod.js",
    "rollbar:sourcemap": "gulp rollbar:sourcemap",
    "rollbar:deploy:dev": "NODE_ENV=dev node scripts/rollbar_deploy.js",
    "rollbar:deploy:prod": "NODE_ENV=prod node scripts/rollbar_deploy.js",
    "stage:rev": "node scripts/rev.js",
    "stage": "npm run build && npm run stage:rev",
    "cache:purge": "node scripts/purge_cache.js",
    "deploy:copy": "node scripts/deploy.js && npm run chmod",
    "deploy": "npm run stage && npm run rollbar:sourcemap && npm run deploy:copy && npm run rollbar:deploy:prod && npm run cache:purge",
    "watch": "watch 'npm run www:dev' src",
    "render": "python scripts/render.py"
  },
  "homepage": "https://github.com/devilesk/dota-hero-calculator#readme",
  "devDependencies": {
    "bootstrap": "^3.3.7",
    "brfs": "^1.4.3",
    "browserify": "^13.1.0",
    "browserify-replace": "^0.9.0",
    "browserify-shim": "^3.8.12",
    "chart.js": "^1.1.1",
    "chmod": "^0.2.1",
    "clean-css": "^3.4.21",
    "del": "^2.2.1",
    "dota-hero-calculator-library": "^0.16.0",
    "git-rev-sync": "^1.6.0",
    "gulp": "^3.9.1",
    "gulp-rollbar": "^0.1.3",
    "gulp-sourcemaps": "^1.6.0",
    "imagemin-cli": "^3.0.0",
    "jquery": "^3.1.0",
    "jquery-ui": "^1.12.0",
    "knockout": "^3.4.0",
    "knockout-mapping": "^2.6.0",
    "minifyify": "^7.3.4",
    "preprocess": "^3.1.0",
    "replace": "^0.3.0",
    "request": "^2.74.0",
    "rollbar-browser": "^1.9.2",
    "watch": "^1.0.1"
  },
  "dependencies": {},
  "browserify": {
    "transform": [
      "browserify-shim"
    ]
  },
  "browserify-shim": {
    "knockout": "global:ko",
    "jquery": "global:$",
    "jquery-ui": {
      "depends": [
        "jquery:jQuery"
      ]
    }
  }
}
